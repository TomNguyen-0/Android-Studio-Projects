<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.themeactionbar">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        >

        <!--
        This only works for android three and above.
        @android:style/Theme.Holo is a dark theme.
        @android:style/Theme.Holo.Light is the light theme.
        android:theme="@android:style/Theme.Holo.Light.DarkActionBar" Is for the whole application
        -->
        <activity android:name=".MainActivity"
            android:theme="@android:style/Theme.Holo.Light.DarkActionBar"
            >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".SecondActivity"
            android:theme="@android:style/Theme.Holo"
            ></activity>
    </application>

</manifest>